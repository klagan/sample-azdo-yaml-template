 stages:
  - stage:
    displayName: 'gamma NOT selected message'

    jobs:
    - job:
      displayName: 'print NOT message'
      steps:
      - bash: |
            which bash
            echo Hello $name
        displayName: 'bash NOT gamma'

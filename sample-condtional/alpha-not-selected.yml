 stages:
  - stage:
    displayName: 'alpha NOT selected message'

    jobs:
    - job:
      displayName: 'print NOT message'
      steps:
      - bash: |
            which bash
            echo Hello $name
        displayName: 'bash NOT alpha'

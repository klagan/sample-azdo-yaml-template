 stages:
  - stage:
    displayName: 'gamma selected message'

    jobs:
    - job:
      displayName: 'print nice message'
      steps:
      - bash: |
            which bash
            echo Hello $name
        displayName: 'bash gamma'

 stages:
  - stage:
    displayName: 'beta NOT selected message'

    jobs:
    - job:
      displayName: 'print nice message'
      steps:
      - bash: |
            which bash
            echo Hello $name
        displayName: 'bash NOT beta'
